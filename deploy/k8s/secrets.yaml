# NOTE: This is a template. In production, use external secret management (Vault, AWS Secrets Manager, etc.)
# Never commit actual secrets to version control

apiVersion: v1
kind: Secret
metadata:
  name: neo4j-credentials
  namespace: baggage-ai
type: Opaque
stringData:
  username: neo4j
  password: REPLACE_WITH_VAULT_SECRET  # Use Vault or external secret manager
---
apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: baggage-ai
type: Opaque
stringData:
  password: REPLACE_WITH_VAULT_SECRET  # Use Vault or external secret manager
---
apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  namespace: baggage-ai
type: Opaque
stringData:
  worldtracer_api_key: REPLACE_WITH_VAULT_SECRET
  dcs_api_key: REPLACE_WITH_VAULT_SECRET
  courier_api_key: REPLACE_WITH_VAULT_SECRET
  notification_api_key: REPLACE_WITH_VAULT_SECRET
---
apiVersion: v1
kind: Secret
metadata:
  name: tls-certificate
  namespace: baggage-ai
type: kubernetes.io/tls
stringData:
  tls.crt: |
    -----BEGIN CERTIFICATE-----
    REPLACE_WITH_ACTUAL_CERTIFICATE
    -----END CERTIFICATE-----
  tls.key: |
    -----BEGIN PRIVATE KEY-----
    REPLACE_WITH_ACTUAL_PRIVATE_KEY
    -----END PRIVATE KEY-----
---
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-credentials
  namespace: baggage-ai
type: Opaque
stringData:
  client_id: REPLACE_WITH_VAULT_SECRET
  client_secret: REPLACE_WITH_VAULT_SECRET
  jwt_secret: REPLACE_WITH_VAULT_SECRET
